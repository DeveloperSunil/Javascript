// Template Literals (Template Strings)
// in template literal backtick is come
for(let num=1;num<=10;num++){
     let tableOf = 12;
    // console.log(tableOf+" * "+num+" = "+ tableOf*num);
     console.log(`${tableOf} * ${num} = ${tableOf*num}`);// Template String
}
