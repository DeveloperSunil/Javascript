// There are two updates in ES7
// Array.prototype.includes
// Exponention operator.

// Array.prototype.includes --> it give true or false. if element is present in array it gives true.
// if element is not present then it gives false.
const colors = ['red','green','blue','white'];
const isPresent = colors.includes('red');
console.log(isPresent);

// Exponention operator **
console.log(2**3);